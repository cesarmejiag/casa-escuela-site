import Layout from "../components/Layout";
import Section from "../components/Section";

const covidPolicy = () => {
  return (
    <Layout pageTitle="Covid Policy">
      <Section id="covid-policy"></Section>
    </Layout>
  );
};

export default covidPolicy;
